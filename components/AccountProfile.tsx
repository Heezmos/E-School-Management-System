"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";
export default function AccountProfile({profile,role,roleDetails,school}:{profile:any;role:string;roleDetails?:any;school?:any}){const r=useRouter();const[busy,setBusy]=useState("");async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy("profile");const f=new FormData(e.currentTarget);const res=await fetch("/api/account/profile",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f.entries()))});const d=await res.json();setBusy("");if(!res.ok)return alert(d.error||"Unable to update profile");alert("Profile updated.");r.refresh()}async function password(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy("password");const f=new FormData(e.currentTarget);const p=String(f.get("password")||""),c=String(f.get("confirm_password")||"");if(p!==c){setBusy("");return alert("Passwords do not match.")}const res=await fetch("/api/account/password",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({password:p})});const d=await res.json();setBusy("");if(!res.ok)return alert(d.error||"Unable to change password");(e.currentTarget as HTMLFormElement).reset();alert("Password changed successfully.")}
return <><section className="premium-grid"><article className="premium-panel"><div className="panelhead"><div><h2>Personal Profile</h2><p className="note">Manage the contact information attached to your E-School account.</p></div></div><form className="form-grid" onSubmit={save}><label className="field"><span>First Name</span><input name="first_name" defaultValue={profile?.first_name||""} required/></label><label className="field"><span>Last Name</span><input name="last_name" defaultValue={profile?.last_name||""} required/></label><label className="field"><span>Email</span><input value={profile?.email||""} disabled/><small>Your login email is managed as an account identifier.</small></label><label className="field"><span>Phone</span><input name="phone" defaultValue={profile?.phone||""}/></label><div className="field" style={{alignSelf:"end"}}><button className="premium-button" disabled={!!busy}>{busy==="profile"?"Saving...":"Save Profile"}</button></div></form></article><article className="premium-panel"><div className="panelhead"><div><h2>Account & School Identity</h2><p className="note">Verified information assigned by your school or platform administrator.</p></div></div><div className="action"><div><strong>Role</strong><small>{role.replaceAll("_"," ")}</small></div><span className="pill greenpill">Active</span></div>{school?<><div className="action"><div><strong>School</strong><small>{school.name}</small></div></div><div className="action"><div><strong>School Code</strong><small>{school.school_code||"—"}</small></div></div></>:null}{roleDetails?.teacher_number?<div className="action"><div><strong>Teacher Number</strong><small>{roleDetails.teacher_number}</small></div></div>:null}{roleDetails?.student_number?<div className="action"><div><strong>Student Number</strong><small>{roleDetails.student_number}</small></div></div>:null}{roleDetails?.specialization?<div className="action"><div><strong>Specialization</strong><small>{roleDetails.specialization}</small></div></div>:null}{roleDetails?.occupation?<div className="action"><div><strong>Occupation</strong><small>{roleDetails.occupation}</small></div></div>:null}</article></section><section className="premium-panel" style={{marginTop:18}}><div className="panelhead"><div><h2>Password & Security</h2><p className="note">Choose a strong password that you do not use on another service.</p></div></div><form className="form-grid" onSubmit={password}><label className="field"><span>New Password</span><input name="password" type="password" minLength={8} required autoComplete="new-password"/><small>Use at least 8 characters.</small></label><label className="field"><span>Confirm New Password</span><input name="confirm_password" type="password" minLength={8} required autoComplete="new-password"/></label><div className="field" style={{alignSelf:"end"}}><button className="premium-button" disabled={!!busy}>{busy==="password"?"Updating...":"Change Password"}</button></div></form></section></>}
