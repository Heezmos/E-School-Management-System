"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";
const days=['','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'];
export default function AcademicCalendarWorkspace({mode,events,timetable,years,terms,classes,subjects,teachers}:{mode:'admin'|'viewer';events:any[];timetable:any[];years:any[];terms:any[];classes:any[];subjects:any[];teachers:any[]}){const r=useRouter();const[busy,setBusy]=useState('');async function post(path:string,e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(path);const f=new FormData(e.currentTarget);const res=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f.entries()))});const d=await res.json();setBusy('');if(!res.ok)return alert(d.error||'Unable to save');(e.currentTarget as HTMLFormElement).reset();r.refresh()}async function remove(path:string,id:string){if(!confirm('Remove this item?'))return;setBusy(id);const res=await fetch(path,{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});const d=await res.json();setBusy('');if(!res.ok)return alert(d.error||'Unable to remove');r.refresh()}
return <>{mode==='admin'?<div className="premium-grid"><section className="premium-panel"><div className="panelhead"><div><h2>Add Calendar Event</h2><p className="note">Publish holidays, exams, meetings, deadlines and school events.</p></div></div><form className="form-grid" onSubmit={e=>post('/api/school-admin/calendar',e)}><label className="field"><span>Title</span><input name="title" required/></label><label className="field"><span>Type</span><select name="event_type"><option value="general">General</option><option value="holiday">Holiday</option><option value="exam">Exam</option><option value="assessment">Assessment</option><option value="meeting">Meeting</option><option value="deadline">Deadline</option><option value="school_event">School Event</option></select></label><label className="field"><span>Academic Year</span><select name="academic_year_id"><option value="">None</option>{years.map(y=><option key={y.id} value={y.id}>{y.name}</option>)}</select></label><label className="field"><span>Term</span><select name="term_id"><option value="">None</option>{terms.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></label><label className="field"><span>Class</span><select name="class_id"><option value="">Whole school</option>{classes.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label className="field"><span>Start</span><input name="starts_at" type="datetime-local" required/></label><label className="field"><span>End</span><input name="ends_at" type="datetime-local"/></label><label className="field" style={{gridColumn:'1/-1'}}><span>Description</span><textarea name="description" rows={3}/></label><button className="premium-button" disabled={!!busy}>Publish Event</button></form></section><section className="premium-panel"><div className="panelhead"><div><h2>Add Timetable Entry</h2><p className="note">Schedule recurring weekly lessons for a class.</p></div></div><form className="form-grid" onSubmit={e=>post('/api/school-admin/timetable',e)}><label className="field"><span>Academic Year</span><select name="academic_year_id" required>{years.map(y=><option key={y.id} value={y.id}>{y.name}</option>)}</select></label><label className="field"><span>Term</span><select name="term_id"><option value="">All year</option>{terms.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></label><label className="field"><span>Class</span><select name="class_id" required>{classes.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label className="field"><span>Subject</span><select name="subject_id" required>{subjects.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label className="field"><span>Teacher</span><select name="teacher_id"><option value="">Unassigned</option>{teachers.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></label><label className="field"><span>Day</span><select name="day_of_week" required>{days.slice(1).map((d,i)=><option key={d} value={i+1}>{d}</option>)}</select></label><label className="field"><span>Start Time</span><input name="start_time" type="time" required/></label><label className="field"><span>End Time</span><input name="end_time" type="time" required/></label><label className="field"><span>Room</span><input name="room"/></label><button className="premium-button" disabled={!!busy}>Add Lesson</button></form></section></div>:null}<section className="premium-panel" style={{marginTop:18}}><div className="panelhead"><div><h2>Academic Calendar</h2><p className="note">Upcoming academic and school events.</p></div></div>{events.length?events.map(e=><div className="action" key={e.id}><div><strong>{e.title}</strong><small>{new Date(e.starts_at).toLocaleString()} · {String(e.event_type).replaceAll('_',' ')}{e.classes?.name?` · ${e.classes.name}`:''}</small>{e.description?<small>{e.description}</small>:null}</div>{mode==='admin'?<button className="mini-btn" disabled={!!busy} onClick={()=>remove('/api/school-admin/calendar',e.id)}>Remove</button>:null}</div>):<div className="empty">No upcoming calendar events.</div>}</section><section className="premium-panel" style={{marginTop:18}}><div className="panelhead"><div><h2>Weekly Timetable</h2><p className="note">Recurring lessons ordered by day and time.</p></div></div><div className="tablewrap"><table className="premium-table"><thead><tr><th>DAY</th><th>TIME</th><th>CLASS</th><th>SUBJECT</th><th>TEACHER</th><th>ROOM</th>{mode==='admin'?<th>ACTION</th>:null}</tr></thead><tbody>{timetable.length?timetable.map(x=><tr key={x.id}><td>{days[x.day_of_week]}</td><td>{String(x.start_time).slice(0,5)}–{String(x.end_time).slice(0,5)}</td><td>{x.classes?.name||'—'}</td><td>{x.subjects?.name||'—'}</td><td>{x.teacher_name||'—'}</td><td>{x.room||'—'}</td>{mode==='admin'?<td><button className="mini-btn" disabled={!!busy} onClick={()=>remove('/api/school-admin/timetable',x.id)}>Remove</button></td>:null}</tr>):<tr><td colSpan={mode==='admin'?7:6}>No timetable entries yet.</td></tr>}</tbody></table></div></section></>}
