"use client";
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
export default function SchoolLifecycleControls({school}:{school:any}){
 const router=useRouter(); const [busy,setBusy]=useState(false);
 async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);setBusy(true);const res=await fetch(`/api/admin/schools/${school.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f.entries()))});const data=await res.json();setBusy(false);if(!res.ok)return alert(data.error||"Unable to update school.");alert("School settings updated.");router.refresh()}
 return <section className="premium-panel" style={{marginTop:18}}><div className="panelhead"><div><h2>School Lifecycle & Profile</h2><p className="note">Manage official school details and workspace availability.</p></div></div><form className="form-grid" onSubmit={save}><label className="field"><span>School Name</span><input name="name" defaultValue={school.name} required/></label><label className="field"><span>School Code</span><input value={school.school_code} disabled/></label><label className="field"><span>Official Email</span><input name="email" type="email" defaultValue={school.email||""}/></label><label className="field"><span>Phone</span><input name="phone" defaultValue={school.phone||""}/></label><label className="field"><span>City</span><input name="city" defaultValue={school.city||""}/></label><label className="field"><span>Country</span><input name="country" defaultValue={school.country||"Sierra Leone"}/></label><label className="field" style={{gridColumn:"1/-1"}}><span>Address</span><input name="address" defaultValue={school.address||""}/></label><label className="field"><span>Workspace Status</span><select name="status" defaultValue={school.status}><option value="active">Active</option><option value="suspended">Suspended</option></select></label><div className="field" style={{alignSelf:"end"}}><button className="premium-button" disabled={busy}>{busy?"Saving...":"Save School Settings"}</button></div></form></section>
}
