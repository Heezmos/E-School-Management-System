"use client";import{useRouter}from"next/navigation";
export default function AnnouncementInbox({items}:{items:any[]}){const r=useRouter();async function read(id:string){await fetch('/api/announcements/read',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({notification_id:id})});r.refresh()}return <section className="premium-panel"><div className="panelhead"><div><h2>Announcements & Notifications</h2><p className="note">Official school communications addressed to your account.</p></div></div>{items.length?items.map(x=><div className="action" key={x.id} style={{alignItems:'flex-start'}}><div style={{flex:1}}><div style={{display:'flex',gap:8,alignItems:'center',flexWrap:'wrap'}}><strong>{x.announcements?.title}</strong>{!x.read_at?<span className="pill amberpill">New</span>:null}<span className={`pill ${x.announcements?.priority==='urgent'?'amberpill':'greenpill'}`}>{x.announcements?.priority}</span></div><small>{new Date(x.announcements?.publish_at||x.created_at).toLocaleString()}</small><p style={{whiteSpace:'pre-wrap',marginBottom:0}}>{x.announcements?.message}</p></div>{!x.read_at?<button className="mini-btn" onClick={()=>read(x.id)}>Mark read</button>:null}</div>):<div className="empty">No announcements have been sent to you yet.</div>}</section>}
