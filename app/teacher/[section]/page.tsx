import { redirect } from "next/navigation";
import DashboardShell from "@/components/DashboardShell";
import TeacherAcademicWorkspace from "@/components/TeacherAcademicWorkspace";
import { getPrimaryRole } from "@/lib/auth";
import { createClient } from "@/lib/supabase/server";
const nav=["Dashboard","My Classes","Assessments","Scores","Attendance","Student Feedback","Analytics","Announcements"];
export default async function TeacherSectionPage({params}:{params:Promise<{section:string}>}){const ctx=await getPrimaryRole();if(ctx.role!=="teacher")redirect("/");const {section}=await params;const title=section.split("-").map(w=>w.charAt(0).toUpperCase()+w.slice(1)).join(" ");if(section==="assessments"||section==="scores"){const s=await createClient();const {data:t}=await s.from("teachers").select("id").eq("profile_id",ctx.user.id).eq("school_id",ctx.schoolId).maybeSingle();if(!t)return <DashboardShell schoolName={ctx.schoolName||"School Workspace"} roleLabel="Teacher" title={title} subtitle="Teacher academic workspace" nav={nav}><div className="empty">Teacher profile not found.</div></DashboardShell>;const {data:assignments}=await s.from("teacher_assignments").select("id,academic_year_id,term_id,class_id,subject_id,classes(name),subjects(name)").eq("teacher_id",t.id).eq("status","active");const yearIds=[...new Set((assignments||[]).map((a:any)=>a.academic_year_id))];const {data:terms}=yearIds.length?await s.from("terms").select("id,name,academic_year_id").in("academic_year_id",yearIds):{data:[]};const {data:categories}=yearIds.length?await s.from("assessment_categories").select("id,name,default_weight,academic_year_id,term_id").eq("school_id",ctx.schoolId).in("academic_year_id",yearIds).eq("status","active"):{data:[]};const {data:assessments}=await s.from("assessments").select("id,title,academic_year_id,class_id,subject_id,maximum_score,weight_percentage,status,classes(name),subjects(name)").eq("school_id",ctx.schoolId).eq("teacher_id",t.id).order("created_at",{ascending:false});const classIds=[...new Set((assignments||[]).map((a:any)=>a.class_id))];const {data:students}=classIds.length?await s.from("student_enrollments").select("student_id,class_id,academic_year_id,students(first_name,last_name,student_number)").eq("school_id",ctx.schoolId).eq("enrollment_status","active").in("class_id",classIds):{data:[]};const ids=(assessments||[]).map((a:any)=>a.id);const {data:scores}=ids.length?await s.from("student_scores").select("assessment_id,student_id,raw_score,teacher_remark").in("assessment_id",ids):{data:[]};return <DashboardShell schoolName={ctx.schoolName||"School Workspace"} roleLabel="Teacher" title="Assessment & Score Workspace" subtitle="Create assessments, record marks and submit results for approval" nav={nav}><TeacherAcademicWorkspace assignments={assignments||[]} categories={categories||[]} terms={terms||[]} assessments={assessments||[]} students={students||[]} scores={scores||[]}/></DashboardShell>}return <DashboardShell schoolName={ctx.schoolName||"School Workspace"} roleLabel="Teacher" title={title} subtitle="Teacher academic workspace" nav={nav}><section className="panel"><h2>{title}</h2><div className="empty">This teacher module is connected and will be implemented in the next academic workflow.</div></section></DashboardShell>}