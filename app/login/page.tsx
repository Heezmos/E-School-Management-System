"use client";import{FormEvent,useState}from"react";import Link from"next/link";import{useRouter}from"next/navigation";import{createClient}from"@/lib/supabase/client";
export default function LoginPage(){const router=useRouter(),supabase=createClient();const[loading,setLoading]=useState(false),[error,setError]=useState("");async function record(action:string,email:string){try{await fetch('/api/security-events',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,email})})}catch{}}async function signIn(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget),email=String(f.get('email')||'').trim().toLowerCase(),password=String(f.get('password')||'');const{error}=await supabase.auth.signInWithPassword({email,password});if(error){await record('login_failed',email);setError('We could not sign you in. Check your email and password and try again.');setLoading(false);return}await record('login_success',email);router.replace('/');router.refresh()}return <main className="edu-access login-access"><section className="access-story"><Link href="/" className="access-logo"><span>E</span><b>E-School<small>Smart School Management</small></b></Link><div className="access-story-copy"><span className="access-kicker">WELCOME TO YOUR DIGITAL SCHOOL</span><h1>School life.<br/>Connected in one place.</h1><p>Secure access for school administrators, teachers, students and parents — each with a workspace designed around their role.</p><div className="school-scene"><div className="scene-board"><small>TODAY AT SCHOOL</small><b>Learning. Progress. Connection.</b><div><span>94%</span><span>78%</span><span>24</span></div></div><div className="scene-people"><i>ADMIN</i><i>TEACHER</i><i>STUDENT</i><i>PARENT</i></div></div></div><div className="access-quote">One account. The right role. Your school information stays where it belongs.</div></section><section className="access-form-side login-form-side"><div className="access-top"><Link href="/">← E-School Home</Link><span>School not on E-School? <Link href="/apply">Apply here</Link></span></div><form className="login-card" onSubmit={signIn}><div className="access-heading"><span>SECURE PORTAL ACCESS</span><h2>Welcome back.</h2><p>Enter the credentials provided by your school or E-School administrator.</p></div><label className="access-field"><span>Email address</span><input name="email" type="email" required autoComplete="email" placeholder="name@school.edu"/></label><label className="access-field"><span>Password</span><input name="password" type="password" required autoComplete="current-password" placeholder="Enter your password"/></label><div className="login-helper"><span>Your role is identified automatically after sign-in.</span></div>{error?<div className="access-error">{error}</div>:null}<button className="access-submit" disabled={loading}>{loading?'Opening your workspace...':'Sign in to E-School →'}</button><div className="secure-note"><span>◆</span><p><b>Secure role-based access</b><br/>You will only see information authorized for your school and assigned role.</p></div></form><p className="access-foot">Need access? Contact your school administrator. Schools interested in joining E-School should use the school application form.</p></section></main>}
