import { redirect } from "next/navigation";
import DashboardShell from "@/components/DashboardShell";import SuperAdminSchools from "@/components/SuperAdminSchools";import SuperAdminApplications from "@/components/SuperAdminApplications";import SuperAdminSubscriptions from "@/components/SuperAdminSubscriptions";import SuperAdminPlatformUsers from "@/components/SuperAdminPlatformUsers";import SuperAdminAuditLogs from "@/components/SuperAdminAuditLogs";import SuperAdminAnalytics from "@/components/SuperAdminAnalytics";import SuperAdminSupport from "@/components/SuperAdminSupport";import SuperAdminSettings from "@/components/SuperAdminSettings";import {getPrimaryRole}from"@/lib/auth";import{createClient}from"@/lib/supabase/server";
const nav=["Dashboard","Schools","Applications","Subscriptions","Platform Users","Analytics","Support","Audit Logs","Settings"];const sectionTitles:Record<string,string>={schools:"Schools",applications:"School Applications",subscriptions:"Subscriptions","platform-users":"Platform Users",analytics:"Platform Analytics",support:"Support Center","audit-logs":"Audit Logs",settings:"Platform Settings"};
export default async function SuperAdminSectionPage({params}:{params:Promise<{section:string}>}){const ctx=await getPrimaryRole();if(ctx.role!=="super_admin")redirect("/");const{section}=await params;const title=sectionTitles[section]||"Super Admin";const supabase=await createClient();let schools:any[]=[],applications:any[]=[],plans:any[]=[],subscriptions:any[]=[],payments:any[]=[],platformUsers:any[]=[],auditLogs:any[]=[],students:any[]=[],teachers:any[]=[],classes:any[]=[],assessments:any[]=[],attendance:any[]=[],analyticsUsers:any[]=[],supportTickets:any[]=[],scores:any[]=[],reportCards:any[]=[],subjects:any[]=[],terms:any[]=[],years:any[]=[],settings:any[]=[];
if(section==="schools"){const{data}=await supabase.from("schools").select("id,name,school_code,email,phone,city,country,status,created_at").order("created_at",{ascending:false});schools=data||[]}
if(section==="applications"){const{data}=await supabase.from("school_applications").select("id,school_name,school_code,contact_name,contact_email,contact_phone,address,city,country,notes,status,reviewed_at,created_at").order("created_at",{ascending:false});applications=data||[]}
if(section==="subscriptions"){const[a,b,c,d]=await Promise.all([supabase.from("schools").select("id,name,school_code,status").order("name"),supabase.from("subscription_plans").select("*").order("created_at",{ascending:false}),supabase.from("school_subscriptions").select("id,school_id,plan_id,status,starts_at,ends_at,amount,currency,notes,created_at,schools(name,school_code),subscription_plans(name,billing_cycle)").order("created_at",{ascending:false}),supabase.from("subscription_payments").select("id,school_subscription_id,school_id,amount,currency,payment_date,payment_method,reference,period_start,period_end,status,notes,created_at,schools(name,school_code)").order("payment_date",{ascending:false})]);schools=a.data||[];plans=b.data||[];subscriptions=c.data||[];payments=d.data||[]}
if(section==="platform-users"){const{data}=await supabase.from("user_school_roles").select("id,user_id,school_id,role,is_active,created_at,profiles:user_id(first_name,last_name,email,phone,is_active),schools(name,school_code)").order("created_at",{ascending:false});platformUsers=(data||[]).map((x:any)=>({role_id:x.id,user_id:x.user_id,school_id:x.school_id,role:x.role,role_active:x.is_active,created_at:x.created_at,first_name:x.profiles?.first_name,last_name:x.profiles?.last_name,email:x.profiles?.email,phone:x.profiles?.phone,profile_active:x.profiles?.is_active??true,school_name:x.schools?.name,school_code:x.schools?.school_code}))}
if(section==="audit-logs"){const{data}=await supabase.from("audit_logs").select("id,school_id,user_id,action,entity_type,entity_id,old_values,new_values,reason,created_at,profiles:user_id(first_name,last_name,email),schools(name,school_code)").order("created_at",{ascending:false}).limit(1000);auditLogs=(data||[]).map((x:any)=>({...x,actor_name:x.profiles?`${x.profiles.first_name||""} ${x.profiles.last_name||""}`.trim():null,actor_email:x.profiles?.email,school_name:x.schools?.name,school_code:x.schools?.school_code}))}
if(section==="analytics"){const r=await Promise.all([supabase.from("schools").select("id,name,school_code,status").order("name"),supabase.from("user_school_roles").select("user_id,school_id,role,is_active"),supabase.from("students").select("id,school_id,student_number,first_name,last_name,status"),supabase.from("teachers").select("id,school_id,status"),supabase.from("classes").select("id,school_id,academic_year_id,name,status"),supabase.from("assessments").select("id,school_id,academic_year_id,term_id,class_id,subject_id,status"),supabase.from("attendance_records").select("status,attendance_sessions!inner(school_id,academic_year_id,term_id)"),supabase.from("school_subscriptions").select("id,school_id,status,starts_at,ends_at"),supabase.from("subscription_payments").select("id,school_id,amount,currency,status,payment_date"),supabase.from("student_scores").select("id,school_id,student_id,percentage_score,assessments!inner(subject_id,class_id,academic_year_id,term_id)"),supabase.from("report_cards").select("id,school_id,student_id,academic_year_id,term_id,class_id,overall_percentage,attendance_percentage,status,students(first_name,last_name,student_number),classes(name),schools(name)"),supabase.from("subjects").select("id,school_id,name"),supabase.from("terms").select("id,school_id,academic_year_id,name,sequence_number"),supabase.from("academic_years").select("id,school_id,name,is_current")]);schools=r[0].data||[];analyticsUsers=r[1].data||[];students=r[2].data||[];teachers=r[3].data||[];classes=r[4].data||[];assessments=r[5].data||[];attendance=(r[6].data||[]).map((x:any)=>({status:x.status,school_id:x.attendance_sessions?.school_id,academic_year_id:x.attendance_sessions?.academic_year_id,term_id:x.attendance_sessions?.term_id}));subscriptions=r[7].data||[];payments=r[8].data||[];scores=(r[9].data||[]).map((x:any)=>({...x,subject_id:x.assessments?.subject_id,class_id:x.assessments?.class_id,academic_year_id:x.assessments?.academic_year_id,term_id:x.assessments?.term_id}));reportCards=(r[10].data||[]).map((x:any)=>({...x,student_name:x.students?`${x.students.first_name||""} ${x.students.last_name||""}`.trim():null,student_number:x.students?.student_number,class_name:x.classes?.name,school_name:x.schools?.name}));subjects=r[11].data||[];terms=r[12].data||[];years=r[13].data||[]}
if(section==="support"){const{data}=await supabase.from("support_tickets").select("id,ticket_number,school_id,created_by,subject,category,priority,status,description,resolution_summary,last_response_at,resolved_at,closed_at,created_at,updated_at,schools(name,school_code),profiles:created_by(first_name,last_name,email),support_ticket_messages(id,sender_id,message,is_internal,created_at,profiles:sender_id(first_name,last_name,email))").order("updated_at",{ascending:false});supportTickets=(data||[]).map((t:any)=>({...t,school_name:t.schools?.name,school_code:t.schools?.school_code,creator_name:t.profiles?`${t.profiles.first_name||""} ${t.profiles.last_name||""}`.trim():null,creator_email:t.profiles?.email,messages:(t.support_ticket_messages||[]).filter((m:any)=>!m.is_internal).sort((a:any,b:any)=>new Date(a.created_at).getTime()-new Date(b.created_at).getTime()).map((m:any)=>({...m,sender_name:m.profiles?`${m.profiles.first_name||""} ${m.profiles.last_name||""}`.trim():null,sender_email:m.profiles?.email}))}))}
if(section==="settings"){const{data}=await supabase.from("platform_settings").select("setting_key,category,label,description,value,updated_at").order("category").order("label");settings=data||[]}
return <DashboardShell schoolName="Platform Control Center" roleLabel="Super Admin" title={title} subtitle="Platform-wide administration" nav={nav}>{section==="schools"?<SuperAdminSchools schools={schools}/>:section==="applications"?<SuperAdminApplications applications={applications}/>:section==="subscriptions"?<SuperAdminSubscriptions plans={plans} schools={schools} subscriptions={subscriptions} payments={payments}/>:section==="platform-users"?<SuperAdminPlatformUsers users={platformUsers}/>:section==="analytics"?<SuperAdminAnalytics schools={schools} users={analyticsUsers} students={students} teachers={teachers} classes={classes} assessments={assessments} attendance={attendance} subscriptions={subscriptions} payments={payments} scores={scores} reportCards={reportCards} subjects={subjects} terms={terms} years={years}/>:section==="support"?<SuperAdminSupport tickets={supportTickets}/>:section==="audit-logs"?<SuperAdminAuditLogs logs={auditLogs}/>:section==="settings"?<SuperAdminSettings settings={settings}/>:<section className="panel"><h2>{title}</h2><div className="empty">Unknown module.</div></section>}</DashboardShell>}
