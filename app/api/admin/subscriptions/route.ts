import{NextResponse}from"next/server";import{getPrimaryRole}from"@/lib/auth";import{createAdminClient}from"@/lib/supabase/admin";
function addBillingPeriod(start:string,cycle:string){const date=new Date(`${start}T00:00:00Z`);if(Number.isNaN(date.getTime()))return null;if(cycle==="monthly")date.setUTCMonth(date.getUTCMonth()+1);else if(cycle==="termly")date.setUTCMonth(date.getUTCMonth()+4);else if(cycle==="annual")date.setUTCFullYear(date.getUTCFullYear()+1);else return null;date.setUTCDate(date.getUTCDate()-1);return date.toISOString().slice(0,10)}
export async function POST(request:Request){const ctx=await getPrimaryRole();if(ctx.role!=="super_admin")return NextResponse.json({error:"Super Admin access required."},{status:403});const body=await request.json();if(!body.school_id||!body.plan_id||!body.starts_at)return NextResponse.json({error:"School, plan and start date are required."},{status:400});const admin=createAdminClient();const{data:plan}=await admin.from("subscription_plans").select("price,currency,billing_cycle,trial_days").eq("id",body.plan_id).maybeSingle();if(!plan)return NextResponse.json({error:"Subscription plan not found."},{status:404});const calculatedEnd=body.ends_at||addBillingPeriod(body.starts_at,plan.billing_cycle);const status=body.status||"active";if(!["trial","active","past_due","suspended"].includes(status))return NextResponse.json({error:"Invalid starting subscription status."},{status:400});await admin.from("school_subscriptions").update({status:"cancelled",updated_at:new Date().toISOString()}).eq("school_id",body.school_id).in("status",["trial","active","past_due","suspended"]);const payload={school_id:body.school_id,plan_id:body.plan_id,status,starts_at:body.starts_at,ends_at:calculatedEnd,amount:body.amount===""||body.amount==null?plan.price:Number(body.amount),currency:String(body.currency||plan.currency||"SLE").toUpperCase(),notes:String(body.notes||"").trim()||null,created_by:ctx.user.id};if(!Number.isFinite(Number(payload.amount))||Number(payload.amount)<0)return NextResponse.json({error:"Subscription amount must be a valid non-negative number."},{status:400});const{data,error}=await admin.from("school_subscriptions").insert(payload).select("id").single();if(error)return NextResponse.json({error:error.message},{status:400});await admin.from("audit_logs").insert({user_id:ctx.user.id,school_id:body.school_id,action:"assign_school_subscription",entity_type:"school_subscription",entity_id:data.id,new_values:{plan_id:body.plan_id,status,starts_at:body.starts_at,ends_at:calculatedEnd,amount:payload.amount,currency:payload.currency},reason:"Super Admin assigned subscription"});return NextResponse.json({ok:true})}
export async function PATCH(request:Request){const ctx=await getPrimaryRole();if(ctx.role!=="super_admin")return NextResponse.json({error:"Super Admin access required."},{status:403});const body=await request.json();const allowed=new Set(["trial","active","past_due","suspended","cancelled","expired"]);if(!body.id||!allowed.has(body.status))return NextResponse.json({error:"Valid subscription and status are required."},{status:400});const admin=createAdminClient();const{data:current}=await admin.from("school_subscriptions").select("school_id,status,ends_at").eq("id",body.id).maybeSingle();if(!current)return NextResponse.json({error:"Subscription not found."},{status:404});const{error}=await admin.from("school_subscriptions").update({status:body.status,updated_at:new Date().toISOString()}).eq("id",body.id);if(error)return NextResponse.json({error:error.message},{status:400});await admin.from("audit_logs").insert({user_id:ctx.user.id,school_id:current.school_id,action:"change_subscription_status",entity_type:"school_subscription",entity_id:body.id,old_values:{status:current.status,ends_at:current.ends_at},new_values:{status:body.status,ends_at:current.ends_at},reason:"Super Admin changed subscription status"});return NextResponse.json({ok:true})}
